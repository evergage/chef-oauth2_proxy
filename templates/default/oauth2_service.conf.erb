description "Oauth2 Proxy for <%= @name %>"

start on filesystem
stop on runlevel [!2345]

respawn
respawn limit 10 2

script
  exec sudo -u <%= @user %> /usr/local/bin/oauth2_proxy -config="<%= @config_path %>"
end script

